# tables

Folder for all tables (if you use R, often stored as Rds files)

You can create further sub-folders if that makes sense.

For tables that need to be reviewed outside R, it can be useful to save both an
`.rds` file and a plain-text `.csv` or `.md` companion.
