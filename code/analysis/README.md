# analysis

This folder contains code for the main statistical analysis. The example is implemented as an R script, but projects can also use Quarto, Python, Julia, or another language here.

Save generated tables, figures, model summaries, and intermediate outputs under `results/` so final products can load them from a standard location.
