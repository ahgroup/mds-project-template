# utilities

This optional folder is for helper functions or shared code used by more than
one workflow stage.

Keep this folder small. If a helper is only used by one processing, exploration,
or analysis script, it can usually stay next to that script instead.
